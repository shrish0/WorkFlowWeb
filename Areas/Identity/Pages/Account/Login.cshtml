@page
@model LoginModel

<div class="card shadow border-2 mt-5 col-10 col-sm-8 col-md-8 col-xl-6 m-auto  ">
    <div class="card-header bg-dark text-center text-white-50 h2 flex-grow-1">
        Login Here
    </div>
    <div class="card-body">
        <div class="col-12">
            <section>
                <form id="account" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>

                    <div class="form-floating mb-3">
                        <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="name@example.com" />
                        <label asp-for="Input.Email" class="form-label">Email</label>
                        <span asp-validation-for="Input.Email" class="text-danger"></span>
                    </div>

                    <div class="form-floating mb-3">
                        <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true" placeholder="password" />
                        <label asp-for="Input.Password" class="form-label">Password</label>
                        <span asp-validation-for="Input.Password" class="text-danger"></span>
                    </div>

                    <div class="checkbox ">
                        <label asp-for="Input.RememberMe" class="form-label">
                            @Html.DisplayNameFor(m => m.Input.RememberMe)
                            <input class="form-check-input" asp-for="Input.RememberMe" />
                           
                        </label>
                    </div>

                    <div class="d-flex justify-content-evenly mb-3">
                        <button id="login-submit" type="submit" class="col-5 btn btn-lg btn-dark p-lg-3">Log in</button>
                        <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl" class="col-5 btn btn-lg btn-dark text-center">Sign up</a>
                    </div>

                    <div class="d-flex justify-content-between ">
                        <p>
                            <a id="forgot-password" asp-page="./ForgotPassword" >Forgot your password?</a>
                        </p>
                        <p>
                            <a id="resend-confirmation" asp-page="./ResendEmailConfirmation" >Resend email confirmation</a>
                        </p>
                    </div>
                </form>
            </section>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
